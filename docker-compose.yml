services:
  db:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: IJxSzcYxVYux
      POSTGRES_DB: pern-ecommerce
    volumes:
      - db_data:/var/lib/postgresql/data
  pg_admin:
    image: dpage/pgadmin4:latest
    ports:
      - "8080:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: chatpgt7@gmail.com
      PGADMIN_DEFAULT_PASSWORD: testingpassword
    depends_on:
      - db
volumes:
  db_data: